<!DOCTYPE html>
<% include partials/header %>
<script type="text/javascript">
  $(document).ready(function () {
    $.getJSON('/student/get_attendance', function (data) {
      arr = {};
      data.forEach(function (elem) {
        if (! (elem.subject_code in arr)){
          arr[elem.subject_code] = {
            "P" : 0,
            "A" : 0,
            "NA" : 0,
            "subject_name": elem.subject_name,
            "subject_id": elem.subject_id,
          }
        }	
        arr[elem.subject_code][elem.attendance]=arr[elem.subject_code][elem.attendance]+ elem.duration_of_class;
      });
      console.log(arr);
      html = '';
      Object.keys(arr).forEach(function (elem) {
        html += '<tr>';
        html += '<td><a href=\"/student/attendance/'+ arr[elem]['subject_id'] + "\">" + elem + '</a></td>';
        html += '<td>' + arr[elem]['subject_name'] + '</td>';
        html += '<td>' + arr[elem]["P"] + '</td>';
        html += '<td>' + arr[elem]["A"] + '</td>';
        html += '<td>' + (arr[elem]["P"]*100/(arr[elem]["P"] + arr[elem]["A"])).toFixed(2) + '</td>';
        html += '</tr>';
      });
      $('.attendance-table').html(html);
    })
  })
</script>
<div id="grad" class="container student-card card">

  <div class="card-action">
    <div class="row">
      <div class="col-md-3">
        <div class="small-title">SCHOOL</div>
        <div class="small-content school" style="text-transform: uppercase;"><%=user.school%></div>
      </div>
      <div class="col-md-3">
        <div class="small-title">COURSE</div>
        <div class="small-content course"><%= user.course %></div>
      </div>
      <div class="col-md-3">
        <div class="small-title">STREAM</div>
        <div class="small-content stream"><%= user.stream %></div>
      </div>
      <div class="col-md-3">
        <div class="small-title">YEAR OF ADMISSION</div>
        <div class="small-contenty yoa"><%= user.year_of_admission %></div>
      </div>
    </div>
  </div>
  <div class="card-action">
    <div class="row">
      <div class="col-md-12">
        <div class="input-field col-md-6">
          <div class="small-title">EMAIL ID</div>
          <div class="details-field small-content email-id"><%= user.email %></div>
        </div>
        <div class="input-field col-md-6">
          <div class="small-title">PHONE</div>
          <div class="details-field small-content phone"><%= user.phone %></div>
        </div>
      </div>
    </div>
  </div>
  <div class="card-action">
    <div class="row">			
      <a href="/student/change_password" class="change-password btn waves-effect waves-light right">CHANGE
        PASSWORD </a>
        
      </div>
    </div>
    <div class="card-action attendance-report">ATTENDANCE REPORT</div>
    <br>
    <table class="striped centered">
      <thead>
        <tr>
          <th data-field="id">Subject Code</th>
          <th data-field="id">Subject Name</th>
          <th data-field="name">Present</th>
          <th data-field="price">Absent</th>
          <th data-field="price">Percentage</th>
        </tr>
      </thead>
      <tbody class="attendance-table">
      </tbody>
    </table>
    <p>* click on subject code to view details</p>
  </div>
</div>
</div>
<% include partials/footer %>
