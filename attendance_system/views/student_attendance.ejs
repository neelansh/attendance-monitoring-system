<!DOCTYPE html>
<% include partials/header %>
<style type="text/css">
  .heading{
    text-align: center;
    font-size: 2em;
    font-weight: 700;
    color: darkcyan;
    letter-spacing: 0.2em;
  }
</style>
<div class="container">

	<div>
		<h5 class="heading">Student was present/absent for the following lectures:</h5>
		<table class="striped centered">
			<thead>
				<tr>
					<th>Lecture Date</th>
					<th>Present / Absent</th>
					<th>hours of lecture</th>
				</tr>
			</thead>
			<tbody>
				<% for(var i=0 ; i<attendance.length; ++i){ %>
				<tr>
				<% var date = new Date(attendance[i].lecture_timestamp) %>
				<td><%=date.getDate()%>/<%=date.getMonth()%>/<%=date.getFullYear()%></td>
				<td><%=attendance[i].attendance%></td>
				<td><%=attendance[i].duration_of_class%></td>
				</tr>    
				<% } %>
			</tbody>
		</table>	
	</div>

</div>



<% include partials/footer %>